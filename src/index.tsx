import * as React from 'react';
import * as ReactDOM from "react-dom";

import App from './App';


var mountNode = document.getElementById("root");
ReactDOM.render(<App name="Team-flex" />, mountNode);

